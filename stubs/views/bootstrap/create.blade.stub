{{ LayoutStart }}

<div class="container-fluid py-4">
    {{-- Page Header --}}
    <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-4 border-bottom">
        <div>
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb mb-1">
                    <li class="breadcrumb-item"><a href="{{ route('{{ RouteNamePrefix }}{{ model_names }}.index') }}" class="text-decoration-none">{{ ModelNames }}</a></li>
                    <li class="breadcrumb-item active" aria-current="page">Add {{ ModelName }}</li>
                </ol>
            </nav>
            <h1 class="h3 m-0">New {{ ModelName }}</h1>
        </div>
        <div class="btn-toolbar mb-2 mb-md-0">
            <div class="btn-group me-2">
                <a href="{{ route('{{ RouteNamePrefix }}{{ model_names }}.index') }}" class="btn btn-sm btn-outline-secondary">
                    Cancel
                </a>
                <button type="submit" form="create-{{ model_name }}-form" class="btn btn-sm btn-primary">
                    Save {{ ModelName }}
                </button>
            </div>
        </div>
    </div>

    {{-- Session Errors --}}
    @if($errors->any())
    <div class="alert alert-danger" role="alert">
        <ul class="mb-0">
            @foreach($errors->all() as $error)
                <li>{{ $error }}</li>
            @endforeach
        </ul>
    </div>
    @endif

    {{-- Form --}}
    <form id="create-{{ model_name }}-form" action="{{ route('{{ RouteNamePrefix }}{{ model_names }}.store') }}" method="POST">
        @csrf
        @include('pages.{{ RouteNamePrefix }}{{ model-names }}.form')
    </form>
</div>
{{ LayoutEnd }}
